import type { Metadata } from "next";

export const metadata: Metadata = { title: "Campaigns - Nexistry Backend" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
